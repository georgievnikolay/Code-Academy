//използвайте условния оператор за намиране на максималното от две числа
#include<stdio.h>

int main() {
    int nA = 6;
    int nB = 8;

    printf("The higher number is: %d\n", nA >= nB ? nA : nB);

    return 0;
}
