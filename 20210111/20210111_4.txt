#include<stdio.h>

int vdigniBitna (int number, int index);

int main() {
    
    printf("%d\n", vdigniBitna(5, 1));
 
    return 0;
}

int vdigniBitna(int number, int index) {
    int mask = 1;
    mask = mask << index;

    return number | mask;

}