#include<stdio.h>

int main() {
    
    int number = 112;
    
    int mask = 1;
    mask |= mask << 1;
    mask |= mask << 1;
    mask = mask << 4;

    printf("%d\n", number ^ mask);
 
    return 0;
}